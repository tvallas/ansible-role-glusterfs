glusterfs_default_release: centos-release-gluster41
glusterfs_required_packages:
  - "{{ glusterfs_default_release }}"

glusterfs_packages:
  - glusterfs-server
  - glusterfs-client

glusterfs_service: glusterd
